sep=,${-- first line defines fixed separator char; US defaults to "," non-US to ";" }
${foreach movies movie}${movie.title},${movie.mediaInfoAspectRatioAsString},${movie.year},${movie.dateAdded;date(yyyy-MM-dd)},${movie.path}
${end}
